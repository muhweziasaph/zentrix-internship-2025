# Node.js modules (if later expanded with JS/React)
node_modules/
# Environment variables (for any later backend connection)
.env
# Log files
*.log
# System-specific files
.DS_Store
Thumbs.db
# Temporary/Backup files
*.bak
*.tmp
~$*
# Office temporary files (if report is added)
~$*.docx
# VS Code settings (optional to ignore)
.vscode/
# Build/dist folders (for future use with bundlers)
dist/
build/
# Other editor temp files
*.swp
*.swo
